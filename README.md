# DataBase
## Requirements
+ django
+ json2html

## Start
```bash
docker-compose up
python manage.py runserver *YOUR_PORT*
```

## Adminer läuft auf port 8080
datenbank login:
+ username: root
+ password: password
+ database: loginProject

